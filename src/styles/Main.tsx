import { MainStylesInterface } from "../interfaces/StylesInterface";

const MainStyles: MainStylesInterface = ({
  container: {
    width: '80%',
    float: 'right',
  },
  containerSml: {
    width: '100%',
  },
});

export default MainStyles;
